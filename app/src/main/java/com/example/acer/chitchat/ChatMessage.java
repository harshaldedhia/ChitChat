package com.example.acer.chitchat;

public class ChatMessage {
    public boolean is_sent;
    public String message;

    public ChatMessage(boolean is_sent, String message) {
        super();
        this.is_sent = is_sent;
        this.message = message;
    }
}
